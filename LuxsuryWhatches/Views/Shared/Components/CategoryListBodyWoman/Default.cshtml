@using LuxuryWhaches.EntityLayer.Concreate;
@model List<Category>
@foreach (var x in Model.Where(x => x.CategoryStatus == true))
{
    <li><a href="/Default/GetproductWithcategoryIdWoman/@x.CategoryID/">@x.CategoryName</a></li>
}
