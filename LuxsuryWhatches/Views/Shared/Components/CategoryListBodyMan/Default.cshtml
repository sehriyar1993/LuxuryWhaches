@using LuxuryWhaches.EntityLayer.Concreate;
@model List<Category>
@foreach (var x in Model.Where(x => x.CategoryStatus == true))
{
    <li><a href="/Default/GetproductWithcategoryIdMan/@x.CategoryID/">@x.CategoryName</a></li>

}
