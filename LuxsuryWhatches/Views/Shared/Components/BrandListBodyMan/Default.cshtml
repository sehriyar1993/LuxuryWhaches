@using LuxuryWhaches.EntityLayer.Concreate;
@model List<Brand>
@foreach (var x in Model.Where(x => x.BrandStatus == true))
{

    <li><a href="/Default/GetProductForBrandMan/@x.BrandID/">@x.BrandName</a></li>

}
